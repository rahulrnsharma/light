{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Program (Test)",
            "program": "${workspaceFolder}/test.js",
            "cwd": "${workspaceFolder}",
            "autoAttach": "always"
        }
    ]
}